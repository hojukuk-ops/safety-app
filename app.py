import streamlit as st

# ==========================================
# 1. 페이지 설정 (가장 먼저 실행)
# ==========================================
st.set_page_config(
    page_title="안산도시공사 AI 안전보건 플랫폼", 
    page_icon="🏗️", 
    layout="wide"
)

# ==========================================
# 2. 통합 CSS 스타일 (모든 페이지 공통 적용)
# ==========================================
st.markdown("""
<style>
    /* 1. 사이드바 메뉴 텍스트 줄바꿈 & 예쁘게 */
    div[data-testid="stSidebarNav"] span {
        white-space: normal !important;
        line-height: 1.5 !important;
        word-break: keep-all;
        padding-top: 5px;
        padding-bottom: 5px;
    }
    
    /* 2. 모든 페이지의 제목 옆 '클립(🔗)' 아이콘 숨기기 */
    [data-testid="stHeaderActionElements"] {
        display: none !important;
    }
</style>
""", unsafe_allow_html=True)

# ==========================================
# 3. 메뉴 구성 (네비게이션)
# ==========================================
pages = {
    "메인 메뉴": [
        st.Page("home.py", title="홈", icon="🏠"),
    ],
    "안전관리 기능": [
        st.Page("pages/1_📑_도급·용역_안전_도우미.py", title="도급·용역 AI 안전 도우미", icon="📑"),
        st.Page("pages/2_📸_AI_세이프티_렌즈.py", title="AI 세이프티 렌즈 (위험성 평가)", icon="📸"),
        # 👇 [신규] 파일명과 제목을 'AI 근로감독관'으로 변경했습니다!
        st.Page("pages/3_👮_AI_근로감독관.py", title="AI 근로감독관", icon="👮"),
    ]
}

# ==========================================
# 4. 네비게이션 실행 (앱 가동)
# ==========================================
pg = st.navigation(pages)
pg.run()

# ==========================================
# 5. 사이드바 하단 문의처 (고정)
# ==========================================
with st.sidebar:
    st.markdown("---") 
    st.markdown(
        """
        <div style="font-size: 12px; color: #666;">
            <b>📞 시스템 문의 및 제보</b><br>
            <br>
            안전관리팀 주임 진형국<br>
            (내선 4872)
        </div>
        """,
        unsafe_allow_html=True
    )