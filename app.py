import streamlit as st

# ==========================================
# 1. 페이지 설정 (가장 윗줄 필수)
# ==========================================
st.set_page_config(
    page_title="안산도시공사 AI 안전보건 플랫폼", 
    page_icon="🏗️", 
    layout="wide"
)

# ==========================================
# 2. [추가] 사이드바 글자 짤림 방지 CSS
# ==========================================
st.markdown("""
<style>
    /* 사이드바 메뉴 텍스트 줄바꿈 강제 허용 */
    div[data-testid="stSidebarNav"] span {
        white-space: normal !important; /* 길면 다음 줄로 내림 */
        line-height: 1.5 !important;    /* 줄 간격 조정 */
        word-break: keep-all;           /* 단어 단위로 끊기 */
        padding-top: 5px;
        padding-bottom: 5px;
    }
</style>
""", unsafe_allow_html=True)

# ==========================================
# 3. 메뉴 구성 (관제탑)
# ==========================================
pages = {
    "메인 메뉴": [
        st.Page("home.py", title="홈", icon="🏠"),
    ],
    "안전관리 기능": [
        # 님께서 확인하신 파일명 그대로 적용했습니다.
        st.Page("pages/1_📑_도급·용역_안전_도우미.py", title="도급·용역 안전 도우미", icon="📑"),
        st.Page("pages/2_📸_AI_세이프티_렌즈.py", title="AI 위험성 평가 (Lens)", icon="📸"),
    ]
}

# ==========================================
# 4. 네비게이션 실행
# ==========================================
pg = st.navigation(pages)
pg.run()