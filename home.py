import streamlit as st

# [주의] st.set_page_config는 app.py에 있으므로 생략

# ==========================================
# 1. 스타일 설정 (홈 화면 전용 - 버튼 꾸미기)
# ==========================================
st.markdown("""
<style>
    /* 설명 텍스트 스타일 */
    div[data-testid="stVerticalBlock"] p {
        white-space: normal !important;
        word-break: keep-all;
        color: #444; /* 글자색 진하게 */
    }

    /* 버튼(Page Link)을 '카드 제목'처럼 만드는 마법 */
    a[data-testid="stPageLink-NavLink"] {
        border: 2px solid #007bff;   /* 파란 테두리 (강조) */
        background-color: #f0f8ff;   /* 아주 연한 파란 배경 */
        border-radius: 10px;         /* 둥근 모서리 */
        padding: 15px !important;    /* 내부 여백 빵빵하게 */
        text-align: center !important; /* 가운데 정렬 */
        box-shadow: 0 4px 6px rgba(0,0,0,0.1); /* 그림자 효과 */
        transition: transform 0.2s;  /* 눌렀을 때 애니메이션 */
    }

    /* 마우스 올렸을 때 효과 */
    a[data-testid="stPageLink-NavLink"]:hover {
        transform: scale(1.02); /* 살짝 커짐 */
        background-color: #e0f0ff;
    }
    
    /* 버튼 안의 글자 스타일 */
    a[data-testid="stPageLink-NavLink"] p {
        font-size: 1.4rem !important; /* 글자 아주 크게 */
        font-weight: 800 !important;  /* 아주 두껍게 */
        color: #0056b3 !important;    /* 파란색 글씨 */
        margin: 0 !important;
    }
    
    /* [삭제됨] 제목 옆 쇠사슬 아이콘 숨기기 코드
       이유: app.py (관제탑)에서 이미 전역 설정으로 숨겼습니다!
    */
</style>
""", unsafe_allow_html=True)

# ==========================================
# 2. 메인 헤더
# ==========================================
st.title("🏗️ 안산도시공사 안전보건 플랫폼")
st.markdown("### 환영합니다! 원하시는 업무를 선택해주세요. 👋")
st.markdown("---")

# 공지사항
with st.container(border=True):
    st.info("""
    📢 **[주요 공지사항]**
            
            1. 도급·용역 계약 시 '도급·용역 안전 도우미' 기능을 적극 활용 바랍니다.
            
            2. 현장 순회 점검 시 'AI 세이프티 렌즈 (위험성평가)' 기능을 적극 활용 바랍니다.
            
            3. 앱 사용 중 발생하는 오류는 안전관리팀으로 문의해주세요.
    """)

st.write("") 

# ==========================================
# 3. 메뉴 바로가기 (제목 = 버튼)
# ==========================================
col1, col2 = st.columns(2)

# [왼쪽] 도급 안전 도우미
with col1:
    with st.container(border=True):
        # 👇 1. 제목 위치에 '이동 버튼'을 배치 (이게 제목이자 버튼입니다!)
        st.page_link(
            "pages/1_📑_도급·용역_안전_도우미.py", 
            label="📑 도급·용역 안전 도우미", 
            use_container_width=True
        )
        
        # 👇 2. 설명은 버튼 아래에 텍스트로만 존재 (클릭 안 됨)
        st.write("") 
        st.write("👉 도급·용역 계약 시 필요한 안전 서류와 절차를 안내받고, 결과 보고서 및 교육일지를 생성합니다.")

# [오른쪽] AI 위험성평가
with col2:
    with st.container(border=True):
        # 👇 1. 제목 위치에 '이동 버튼' 배치
        st.page_link(
            "pages/2_📸_AI_세이프티_렌즈.py", 
            label="📸 AI 위험성평가", 
            use_container_width=True
        )
        
        # 👇 2. 설명
        st.write("")
        st.write("👉 현장 사진을 촬영하면 AI가 위험요인을 분석하고 대책을 알려줍니다.")

# ==========================================
# 4. 푸터
# ==========================================
st.markdown("---")
st.caption("ⓒ Ansan Urban Corporation Safety Team | 시스템 문의: 안전관리팀 주임 진형국(내선 4872)")